# 구현 계획: 권한 코어 + 서버 보안 재정의

- **작성일**: 2026-04-07
- **기반 리서치**: [20260407_권한모델재정의_Research.md](../01-research/20260407_권한모델재정의_Research.md)
- **전제 조건**: VS-1(profiles 컬럼 추가), VS-2(온보딩 페이지), VS-3(로그인 분기) 구현 완료 상태
- **구현 방향**: A안 (role enum 확장: `user` 추가)

---

## 1. 기능 요약

### 이번 범위에 포함되는 것

- `app_role` enum에 `user` 추가 → 4종 (`organizer | manager | user | player`)
- 신규 가입 기본 role을 `user`로 변경 (DEFAULT 변경)
- 기존 `player` 데이터 중 프로필 미완료자를 `user`로 downgrade (backfill)
- DB 함수 `is_player()` 신규 추가
- TypeScript `Role` 타입에 `"user"` 추가 + `isPlayerRole()`, `isUserRole()` 유틸 추가
- `requirePlayer()` 서버 가드 추가 (`src/lib/auth/guards.ts`)
- 로그인 후 redirect 분기 재설계 (`user` → `/`, `player` → `/dashboard`)
- 회원가입 후 redirect 재설계 (`user` → `/` 또는 선수 등록 안내)
- `teams` INSERT RLS에 `is_player()` 조건 추가 → `user`는 팀 생성 불가
- `tournament_team_applications` INSERT RLS에 `is_player()` 조건 추가
- `update_user_role` RPC에 `user` 값 허용 추가
- admin/users 서버 액션의 허용 role 목록에 `user` 추가

### 이번 범위에서 제외

- 사이드바/대시보드 user/player UI 분기 상세 구현 (VS-R10, VS-R11)
- 본인인증 UI 및 외부 인증 업체 연동 (VS-R8, VS-R9)
- `role_in_team = 'player' → 'member'` rename (VS-R13, VS-R14)
- `is_team_manager_for_team → is_team_captain_for_team` 함수 rename
- 온보딩 Step UI 리디자인
- `promote_to_player()` RPC 설계 (본인인증 연동 시 필요 — VS-R5)
- 선수 등록 CTA 페이지 UI 구성 (VS-R6, VS-R7)

### 후속 단계에서 확장될 내용

- 본인인증 완료 시 `user → player` 승격 RPC (VS-R5)
- 사이드바 user/player 메뉴 분기 (VS-R10)
- `/dashboard` player 가드 적용 (VS-R11)
- `/team`, `/tournament/[id]/apply` player 가드 적용 (VS-R12)
- 선수 등록 랜딩 CTA 페이지 (VS-R6)

### 완료 후 사용자 이동/후속 처리 방식

- 신규 가입: `user` role 생성 → 랜딩(`/`)으로 이동
- 기존 로그인(user): 랜딩(`/`)으로 이동
- 기존 로그인(player): `/dashboard`로 이동
- `user`가 팀 생성/대회 신청 시도: RLS 차단 + 서버 액션 가드 차단

---

## 2. 라이브러리 검토

**추가 라이브러리 불필요.**

- enum 확장/마이그레이션: Supabase SQL migration으로 처리 가능
- TypeScript 타입 수정: 기존 `src/lib/auth/roles.ts` 직접 수정
- 서버 가드: 기존 `src/lib/auth/guards.ts` 패턴 그대로 추가
- RLS 수정: SQL migration 추가만으로 처리 가능

---

## 3. 변경 파일 목록

### 수정 파일

- `src/lib/auth/roles.ts` — `Role` 타입 확장, `isPlayerRole()` / `isUserRole()` 추가
- `src/lib/auth/guards.ts` — `requirePlayer()` 가드 추가
- `app/(auth)/login/actions.ts` — 로그인 후 분기 로직 재설계
- `app/(auth)/signup/actions.ts` — 가입 후 redirect 변경 (`/` 또는 `/onboarding/profile`)
- `app/(auth)/login/page.tsx` — 이미 로그인 사용자 분기: `user` → `/`, `player` → `/dashboard`
- `app/(app)/admin/users/actions.ts` — 허용 role 목록에 `user` 추가

### 신규 파일

- `supabase/migrations/0215_role_add_user.sql` — enum 확장, DEFAULT 변경, backfill, `is_player()` 추가
- `supabase/migrations/0216_teams_rls_player_guard.sql` — teams INSERT에 `is_player()` 조건 추가
- `supabase/migrations/0217_applications_rls_player_guard.sql` — tournament_team_applications INSERT에 `is_player()` 조건 추가
- `supabase/migrations/0218_update_user_role_add_user.sql` — `update_user_role` RPC에 `user` 허용 추가

---

## 4. 파일별 구현 개요

---

### `supabase/migrations/0215_role_add_user.sql`

**역할**: `app_role` enum 확장 + 기본값/backfill/DB 함수 처리  
**수정 이유**: 현재 enum에 `user`가 없어 신규 가입자와 선수를 구분 불가

**핵심 구조 및 pseudocode**:

```sql
-- 1) storage 정책 임시 삭제 (app_role 타입 참조 의존성 제거)
DROP POLICY IF EXISTS poster_update_organizer ON storage.objects;
DROP POLICY IF EXISTS poster_delete_organizer ON storage.objects;

-- 2) 신규 enum 생성 (4종)
CREATE TYPE public.app_role_v3 AS ENUM ('organizer', 'manager', 'user', 'player');

-- 3) profiles.role DEFAULT 제거 후 타입 교체
ALTER TABLE public.profiles ALTER COLUMN role DROP DEFAULT;
ALTER TABLE public.profiles
  ALTER COLUMN role TYPE public.app_role_v3
  USING (role::text::public.app_role_v3);

-- 4) backfill: 기존 player 중 프로필 미완료자 → user로 downgrade
--    기준: display_name IS NULL OR phone IS NULL
UPDATE public.profiles
  SET role = 'user'
  WHERE role = 'player'
    AND (display_name IS NULL OR phone IS NULL);

-- 5) 신규 기본값 설정
ALTER TABLE public.profiles ALTER COLUMN role SET DEFAULT 'user';

-- 6) enum 이름 교체
DROP TYPE public.app_role;
ALTER TYPE public.app_role_v3 RENAME TO app_role;

-- 7) storage 정책 재생성
CREATE POLICY poster_update_organizer ON storage.objects ...;
CREATE POLICY poster_delete_organizer ON storage.objects ...;

-- 8) is_organizer(), is_manager() — 변경 없음 (재생성 불필요)

-- 9) is_player() 신규 추가
CREATE OR REPLACE FUNCTION public.is_player() RETURNS boolean
LANGUAGE sql SECURITY DEFINER SET search_path = public AS $$
  SELECT EXISTS (
    SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'player'
  )
$$;
```

**주의**:
- `on_auth_user_created` 트리거는 `INSERT INTO profiles (id)`만 실행 → DEFAULT 변경으로 별도 수정 불필요
- `is_organizer()`, `is_manager()` 함수는 `player`, `user` 모두 false를 반환하므로 변경 없음

---

### `supabase/migrations/0216_teams_rls_player_guard.sql`

**역할**: teams 테이블 INSERT 정책에 `is_player()` 조건 추가  
**수정 이유**: 현재 정책이 인증된 모든 사용자(user 포함)의 팀 생성을 허용

**핵심 pseudocode**:

```sql
-- 기존 INSERT 정책 교체
DROP POLICY IF EXISTS "teams_insert_members" ON public.teams;

CREATE POLICY "teams_insert_player_only" ON public.teams
  FOR INSERT WITH CHECK (
    created_by = auth.uid() AND is_player()
  );
```

---

### `supabase/migrations/0217_applications_rls_player_guard.sql`

**역할**: tournament_team_applications INSERT에 `is_player()` 조건 추가  
**수정 이유**: 현재 정책이 captain 확인만 하고 전역 role을 체크하지 않음

**핵심 pseudocode**:

```sql
-- 현재 INSERT 정책 확인 후 교체
-- (0088 또는 0112에서 정의된 정책명 확인 필요)
DROP POLICY IF EXISTS "applications_insert_captain" ON public.tournament_team_applications;

CREATE POLICY "applications_insert_captain_player" ON public.tournament_team_applications
  FOR INSERT WITH CHECK (
    is_team_manager_for_team(team_id)
    AND applied_by = auth.uid()
    AND is_player()
  );
```

---

### `supabase/migrations/0218_update_user_role_add_user.sql`

**역할**: `update_user_role` RPC가 `user` 값을 허용하도록 수정  
**수정 이유**: 현재 RPC가 `player | manager`만 허용 → admin에서 `user`로 역할 변경 불가

**핵심 pseudocode**:

```sql
CREATE OR REPLACE FUNCTION public.update_user_role(
  target_user_id uuid,
  new_role text
)
RETURNS void LANGUAGE plpgsql SECURITY DEFINER ...
AS $$
BEGIN
  IF NOT is_organizer() THEN RAISE EXCEPTION 'Forbidden'; END IF;

  -- 변경: 'user'도 허용
  IF new_role NOT IN ('user', 'player', 'manager') THEN
    RAISE EXCEPTION 'Invalid role';
  END IF;

  -- organizer 보호 유지
  IF EXISTS (SELECT 1 FROM profiles WHERE id = target_user_id AND role = 'organizer') THEN
    RAISE EXCEPTION 'Cannot change organizer role';
  END IF;

  UPDATE profiles SET role = new_role::app_role WHERE id = target_user_id;
END;
$$;
```

---

### `src/lib/auth/roles.ts`

**역할**: 전역 권한 타입 정의 및 역할 판별 유틸  
**수정 이유**: `Role` 타입에 `"user"` 추가, player/user 판별 함수 신규 추가

**핵심 구조**:

```typescript
// 변경: "user" 추가
export type Role = "organizer" | "manager" | "user" | "player";

// 신규 추가
export function isPlayerRole(role: Role | null): boolean {
  return role === "player";
}

export function isUserRole(role: Role | null): boolean {
  return role === "user";
}

// 기존 유지 (변경 없음)
export function isOperationRole(role: Role | null): boolean {
  return role === "organizer" || role === "manager";
}

export function isOrganizerRole(role: Role | null): boolean {
  return role === "organizer";
}
```

---

### `src/lib/auth/guards.ts`

**역할**: 서버 액션용 권한 가드 함수  
**수정 이유**: `player` 전용 기능 서버 보호를 위한 `requirePlayer()` 추가

**핵심 구조**:

```typescript
// 신규 추가
export async function requirePlayer(): Promise<ActionResult> {
  const result = await getUserWithRole();

  if (result.status === "unauthenticated") {
    return { ok: false, error: "로그인이 필요합니다." };
  }
  if (result.status === "error") {
    return { ok: false, error: result.error ?? "사용자 정보를 불러오지 못했습니다." };
  }
  if (result.status === "empty") {
    return { ok: false, error: "프로필이 없습니다." };
  }
  if (result.role !== "player") {
    return { ok: false, error: "선수 등록이 필요합니다." };
  }

  return { ok: true };
}

// 기존 requireOrganizer(), requireOperationRole() 유지
```

---

### `app/(auth)/login/actions.ts`

**역할**: 로그인 서버 액션 — 로그인 후 역할별 redirect  
**수정 이유**: 현재 분기가 `player` 기준으로 설계되어 있어 `user` 상태 처리 없음

**핵심 분기 변경**:

```typescript
// 변경 전
if (isOperationRole(roleResult.role)) redirect("/admin");
const profileResult = await getMyProfile();
if (!isProfileCompleted(profileResult.data)) redirect("/onboarding/profile");
redirect("/dashboard");

// 변경 후
if (isOperationRole(roleResult.role)) redirect("/admin");    // organizer | manager
if (isPlayerRole(roleResult.role)) redirect("/dashboard");   // 선수 등록 완료
redirect("/");  // user: 랜딩 (선수 등록 CTA)

// getMyProfile(), isProfileCompleted() 호출 제거
```

**주의**: 기존 `getMyProfile` / `isProfileCompleted` 기반 분기 완전히 제거. role 기반으로만 판단.

---

### `app/(auth)/signup/actions.ts`

**역할**: 회원가입 서버 액션  
**수정 이유**: 가입 후 redirect가 `/onboarding/profile`로 강제 분기 — `user` 기준으로 변경 필요

**핵심 변경**:

```typescript
// 변경 전: redirect("/onboarding/profile") 또는 "/dashboard"
// 변경 후: redirect("/")  ← 랜딩 (선수 등록 CTA 포함)
```

---

### `app/(auth)/login/page.tsx`

**역할**: 로그인 페이지 — 이미 로그인된 사용자 redirect  
**수정 이유**: 현재 로그인 사용자를 단순히 `/dashboard`로 보내는 로직에 `user` 분기 없음

**핵심 분기**:

```typescript
// 로그인 상태 확인 후
if (isOperationRole(role)) redirect("/admin");
if (isPlayerRole(role)) redirect("/dashboard");
redirect("/");  // user
```

---

### `app/(app)/admin/users/actions.ts`

**역할**: admin 사용자 역할 변경 서버 액션  
**수정 이유**: 현재 TypeScript 타입이 `"player" | "manager"`만 허용 → `"user"` 추가 필요

**핵심 변경**:

```typescript
// 변경 전
type UpdateRoleInput = {
  targetUserId: string;
  newRole: "player" | "manager";
};
if (input.newRole !== "player" && input.newRole !== "manager") { ... }

// 변경 후
type UpdateRoleInput = {
  targetUserId: string;
  newRole: "user" | "player" | "manager";
};
if (!["user", "player", "manager"].includes(input.newRole)) { ... }
```

---

## 5. 구현 시 고려사항

### 기존 구조 유지 vs 공통화

- `getUserWithRole()` / `isOperationRole()` 패턴 그대로 유지
- 신규 `isPlayerRole()`, `requirePlayer()` 추가만으로 충분 — 공통 추상화 불필요

### 클라이언트/서버 검증 위치

- **서버 우선**: `requirePlayer()` 가드를 player 전용 서버 액션(팀 생성, 대회 신청)에 먼저 적용
- **RLS 이중 보호**: 서버 액션 + DB RLS 두 계층에서 `user` 차단
- 클라이언트 UI 분기(버튼 숨김 등)는 이번 범위 제외

### 상태 처리 / 에러 처리

- `requirePlayer()` 반환값이 `ok: false`이면 서버 액션에서 그대로 에러 반환
- `"선수 등록이 필요합니다."` 에러 메시지를 프론트에서 선수 등록 페이지로 유도하는 UI는 후속 단계(VS-R11, VS-R12)에서 처리

### 기존 player 데이터 backfill 기준

- **기준**: `display_name IS NULL OR phone IS NULL` → `user`로 downgrade
- **근거**: 리서치 문서 권장안 — 프로필 미완료 player는 실제 선수 등록을 완료하지 않은 상태
- **주의**: 현재 DB에 실제 player 데이터가 있다면 마이그레이션 전 확인 필요
  - 프로덕션 배포 전 `SELECT count(*) FROM profiles WHERE role='player' AND (display_name IS NULL OR phone IS NULL)` 로 영향 규모 파악 권장

### status = "empty" 예외 처리

- `status="empty"`: `profiles` 행이 없는 상태 (on_auth_user_created 트리거 실패 등 예외)
- 기존 가드 패턴 그대로 유지 — `empty` → `"프로필이 없습니다."` 에러 반환

### user가 팀 생성 / 대회 신청 / dashboard 접근 시도 시

| 시도 | 차단 레이어 | 응답 |
|------|------------|------|
| 팀 생성 (서버 액션) | `requirePlayer()` | `ok: false, error: "선수 등록이 필요합니다."` |
| 팀 생성 (RLS) | `is_player()` 조건 | DB 레벨 INSERT 거부 |
| 대회 신청 (서버 액션) | `requirePlayer()` | 동일 |
| 대회 신청 (RLS) | `is_player()` 조건 | DB 레벨 INSERT 거부 |
| `/dashboard` 접근 | 이번 범위 제외 (VS-R11) | 후속 단계에서 redirect 처리 |

### admin/users 기능과 충돌 가능성

- `update_user_role` RPC: `user` 값 추가 → `user → player` 수동 승격을 admin에서 할 수 있게 됨
- 비즈니스 결정: **선수 등록 플로우(본인인증)를 통해서만 player 승격**이 원칙이라면, admin 수동 승격은 테스트/운영 편의 목적으로만 허용
- 이번 단계에서는 RPC에 `user` 추가만 하고 admin UI 변경(UserRoleForm)은 최소화

### 본인인증 도입 전 임시 승격 정책

- 본인인증 UI 미구현 상태이므로, 현재는 **admin에서 수동으로 `user → player` 승격** 가능하도록 설계
- 또는 별도 임시 서버 액션으로 `promoteToPlayer()`를 추가하는 방안도 가능 (이번 범위 제외)
- 프로덕션 환경에서는 본인인증 완료 후에만 player 승격이 자동으로 이루어지도록 후속 단계에서 강화

-- TODO: 프로덕션 환경 이외에서는 입력해야하는 UI는 모두 구현하고 본인인증 로직만 스킵한다.  프로덕션인지 확인할수 있는 플래그가 있다면 사용, 없다면 프로젝트 내 플래그 생성후 관리할수 있도록 하고 해당 플래그가 develop/staging 이면 스킵할수 있도록 한다. 단, 지금 단계에서 할 작업이 아니라면 추후 UI 구현 단계에서 참고.

### 향후 확장 시 안전한 구조인지

- `is_player()` DB 함수를 기준으로 RLS 확장이 단순함
- `requirePlayer()` 가드를 서버 액션에 추가하는 패턴이 일관적
- `isPlayerRole()` 유틸을 통해 프론트엔드 분기도 일관된 방식으로 추가 가능
- `promote_to_player()` SECURITY DEFINER RPC 추가 시 이 구조와 자연스럽게 연결됨

---

## 6. 구현 순서 (권장)

1. **[DB]** `0215_role_add_user.sql` — enum 확장 + backfill + `is_player()` 추가
2. **[DB]** `0216_teams_rls_player_guard.sql` — teams INSERT 보호
3. **[DB]** `0217_applications_rls_player_guard.sql` — applications INSERT 보호
4. **[DB]** `0218_update_user_role_add_user.sql` — RPC 수정
5. **[TS]** `src/lib/auth/roles.ts` — `Role` 타입 + 유틸 함수 추가
6. **[TS]** `src/lib/auth/guards.ts` — `requirePlayer()` 추가
7. **[TS]** `app/(auth)/login/actions.ts` — 분기 로직 재설계
8. **[TS]** `app/(auth)/signup/actions.ts` — redirect 변경
9. **[TS]** `app/(auth)/login/page.tsx` — 로그인 상태 분기
10. **[TS]** `app/(app)/admin/users/actions.ts` — 허용 role 목록 확장

DB 변경을 먼저 완료한 후 TypeScript 변경을 진행한다.  
TypeScript 컴파일 오류가 발생하는 지점(`role === "player"` 비교 등)은 모두 의도적으로 안전하므로 기존 로직 변경 없이 타입만 확장.
